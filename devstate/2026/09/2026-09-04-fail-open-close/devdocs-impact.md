# Devdocs impact
change: waf-fail-closed

## Units
- Middleware — subsystem — `knowledge/devdocs/core_plugin_middleware.md` (public `failMode` on Config / ServeHTTP)
- Health tracker — subsystem — `knowledge/devdocs/core_plugin_health.md` (already-unhealthy skip follows `failMode`)

## Findings
none.
