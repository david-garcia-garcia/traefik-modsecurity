# Devdocs impact
change: pin-upstream-issue-05

## Units
- Middleware — subsystem — `knowledge/devdocs/core_plugin_middleware.md` (handshake skip + inbound cancel already documented)
- WebSocket handshake — pattern — Language term already on that packet
- Sidecar request — pattern — Language term already on that packet

## Findings
none.
