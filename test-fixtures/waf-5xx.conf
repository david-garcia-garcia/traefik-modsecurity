# Distinctive 503 body so Pester can prove the plugin does not copy sidecar 5xx.
server {
    listen 80;
    server_name _;
    location / {
        default_type text/plain;
        return 503 "sidecar-5xx-marker\n";
    }
}
