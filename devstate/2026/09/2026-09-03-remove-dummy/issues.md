# Issues

- [ ] note large  archive leftover `openspec/changes/inspect-only-crs-sidecar/` (tasks 15/15, still on main, not in `archive/`)
  Why: previous drain work landed without archive. This ticket folds the spec in a new change; moving that folder is a separate catalog cleanup.
